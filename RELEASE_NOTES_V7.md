# یادداشت انتشار نسخهٔ ۷ — سازگار با Next.js

تاریخ: ۲۰۲۶-۰۷-۲۷

## هدف نسخه

این نسخه مستقیماً برای معماری فعلی مخزن، یعنی Next.js App Router، ساخته شده است. برخلاف بستهٔ اشتباه قبلی، هیچ فایل Vite، پوشهٔ `src`، فایل `index.html` یا خروجی `dist` در این نسخه وجود ندارد.

## محل قابلیت‌های تازه

- نشانی صفحهٔ پژوهش‌ها: `/research`
- دسترسی از تمام صفحات: دکمهٔ ثابت «نسخهٔ ۷ — پژوهش‌های تازه» در پایین سمت چپ
- تب اول: «ده پرسش برای همه»
- تب دوم: «انتساب و اصالت‌سنجی»

## ده پرسش عمومی

1. دل یا عقل؟
2. شب یا روز؟
3. غم یا شادی؟
4. من یا ما؟
5. پرسشگرترین شاعر کیست؟
6. گذشته، اکنون یا فردا؟
7. راه یا خانه؟
8. رنگ محبوب شعر فارسی چیست؟
9. کدام پرنده بیشتر دیده می‌شود؟
10. کدام نام جغرافیایی میان شاعران مشترک‌تر است؟

هر پرسش شامل پاسخ کوتاه، شاخص‌های عددی، نمودار، روش محاسبه، نکات برجسته و هشدار تفسیر است.

## پرونده‌های انتساب

- خیام: پاک‌سازی نثر از شعر، هم‌پوشانی مجموعه‌ها، ساختار رباعیات و نمایهٔ مفهومی
- حافظ: تفکیک اشعار منتسب، رتبه‌بندی برای بازبینی و بیان محدودیت نبود دادهٔ نسخه‌شناختی
- تحلیل دوره‌ای: مقایسهٔ جریان‌های قدرت، دین، عشق، طبیعت، زمان و باده در سده‌ها با میانگین‌گیری شاعرمتوازن

## اصول روش‌شناختی

- شباهت محاسباتی به‌عنوان سرنخ استفاده می‌شود، نه حکم قطعی اصالت.
- تفاوت میان یافتهٔ داده‌ای، فرضیهٔ پژوهشی و داوری متخصص حفظ شده است.
- محدودیت‌های ژانر، حجم نمونه، ابهام واژگان و نبود اطلاعات نسخه‌ها در رابط نوشته شده‌اند.
- هیچ پیوندی به محل اصلی فایل TSV در صفحهٔ عمومی پژوهش‌ها نمایش داده نمی‌شود.

## حفاظت از امکانات فعلی

صفحهٔ اصلی، کاوش، کتابخانه، اطلس، پیکرهٔ شعر، Web Worker و فایل TSV فعلی تغییر نمی‌کنند. فقط `app/layout.tsx` جایگزین می‌شود تا مسیر جدید در همهٔ صفحات قابل مشاهده باشد.

## وابستگی‌ها

هیچ وابستگی npm تازه‌ای اضافه نشده و فایل‌های `package.json` و `package-lock.json` نباید تغییر کنند.
